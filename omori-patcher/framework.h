#pragma once

#define WIN32_LEAN_AND_MEAN             // Exclude rarely-used stuff from Windows headers
#define NOMINMAX                        // Fix incompatibility with zasm
#define UNICODE                         // Make sure that win32 macros default to the unicode functions
// Windows Header Files
#include <windows.h>
